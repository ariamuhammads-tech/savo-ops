[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  TZ = "Asia/Jakarta"

# Official Next.js runtime for Netlify (App Router + Node functions).
[[plugins]]
  package = "@netlify/plugin-nextjs"
